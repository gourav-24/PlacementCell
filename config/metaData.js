/* this file stores the data of general picklist being used at multiple places, so we can change them here and 
   they get changed data at every place*/
let data = {
  resultPicklist: ["--None--", "Pass", "Fail", "On-Hold", "Unattended"],
  testData: ["Test data"],
};

module.exports = data;
